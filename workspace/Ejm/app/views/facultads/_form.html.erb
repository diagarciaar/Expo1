<%= form_with(model: facultad, local: true) do |form| %>
  <% if facultad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facultad.errors.count, "error") %> prohibited this facultad from being saved:</h2>

      <ul>
      <% facultad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :facultad_name %>
  </div>

  <div class="field">
    <%= form.label :Nombreplanes %>
    <%= form.text_field :Nombreplanes, id: :facultad_Nombreplanes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
